POSTGRES_USER=financial_user
  # Usuário para acessar o banco de dados PostgreSQL
POSTGRES_PASSWORD=financial_password
  # Senha do banco (MUDE EM PRODUÇÃO!)
POSTGRES_DB=financial_db
  # Nome do banco que será criado

DEBUG=True
  # Django em modo desenvolvimento (mostra erros detalhados)
  # MUDE para False em produção!

SECRET_KEY=sua-chave-secreta...
  # Chave para criptografia do Django
  # GERE UMA NOVA em produção!

CORS_ALLOWED_ORIGINS=http://localhost:3000
  # Permite requisições APENAS dessa URL
  # Bloqueia hackers fazendo requisições de outro lugar

REACT_APP_API_URL=http://localhost:8000/api
  # URL que o React usa para chamar o backend