Django==4.2.7
# Framework web Python (o coração do backend)

djangorestframework==3.14.0
# Permite criar APIs REST facilmente

django-cors-headers==4.3.1
# Permite que o frontend (http://localhost:3000) chame o backend (http://localhost:8000)
# Sem isso, o navegador bloqueia requisições por CORS

psycopg2-binary==2.9.9
# Driver que permite Django se conectar ao PostgreSQL

python-decouple==3.8
# Lê variáveis do arquivo .env

gunicorn==21.2.0
# Servidor web que roda Django em produção

Pillow==10.1.0
# Biblioteca para processar imagens (uploads)